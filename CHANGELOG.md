# 更新记录（Changelog）

版本更新说明。发布新版本时请在本文件顶部追加最新版本记录。

## Unreleased（2026-09-14）

### 审计整改（2026-09-17）
- **F1（严重）**：统一 A（`token_count`）与 B（`token_usage_record`）两代格式的统计合并，修复混合日志下旧格式被整体丢弃导致的数字偏小；规格见 `docs/F1-merge-spec.md`，含 9 条 fixture 回归。
- **F3**：接上 `stale-data`（有历史成功推送但当前超过 15 秒取不到数据）；详情页“运行状态”新增“模型切换 N 次 / 累计重置 N 次”；删除从未赋值的 `waiting-for-codex` 与未被消费的 `outOfOrder`。
- **P1 日志**：监控自管日志 `logs\watch-YYYYMMDD.log`（按日追加、超 1MB 轮转、保留最近 30 份），不再受 `Start-Process -RedirectStandardOutput` 截断影响。
- **P1 守护（已被下方「自启架构调整」取代）**：死因区分（`MONITOR_EXIT` / `IDENTITY_MISMATCH`）、重启前归档 PID/状态到 `runtime-state\history\`（保留 20 份）、身份校验改为 PID+进程名+可执行路径+命令行为主（启动时间降级为辅助）、连续快速退出触发熔断冷却。
- **自启架构调整**：移除常驻 PowerShell 守护进程，改为「计划任务直启 Node 监控」——任务动作不再经过 PowerShell（因此不受执行策略影响、也不依赖管理员权限），重复实例策略为「不启动新实例」（天然单实例），失败重启 3 次 / 间隔 1 分钟。旧任务 `ccm-token-spend-guardian`、启动文件夹快捷方式与 PID/状态文件由新的安装脚本自动清理。实测常驻进程由 2 个降为 1 个，CPU 由约 1.18% 单核降到约 0.2%，内存由约 98 MB 降到约 70 MB。
- **P1 安装/卸载**：`install-autostart.ps1` 在缺少 Node.js 22+ 时直接报错中止（本项目不提供预编译 exe，安装器也不再把 exe 当回退运行时）；安装时若监控实例已在运行会先停掉再拉起，确保加载的是最新脚本。卸载脚本新增 `-RemoveUserScript` 与 `-RemovePlugin`。
- **P1 校验**：新增 `scripts/verify-sync.mjs`，校验源码 / 部署 / 插件 cache / Codex++ user_scripts / 打包 zip 五处一致性。

### 原生 UI 紧凑统计条
- 统计条移动到 Codex 输入区上下文用量圆圈左侧，并使用原生横向布局与主题变量。
- 推送节奏改为：数据变化时立即推送，无变化时每 5 秒固定推送一次。
- 详情弹层的“会话累计”增加请求数量；“运行状态”移动到“上下文使用”下方。
- 关闭方式调整：以「点击弹层外部或右上角 ×」为唯一保证的关闭方式（`pointerdown` + `mousedown` 双重兜底）；`Esc` 降级为增强，因为 Codex 宿主会在按键到达页面之前消费 Escape。
- 从页面负载移除会话文件路径、用户消息片段和轮次明细，只保留展示所需统计字段。
- 当前提问缓存命中、未命中、输出量显示在第二行。
- 监控程序缓存未变化的会话文件，减少每秒重复解析。
- 左键单击统计条打开原生风格详情弹层，展示会话累计、当前提问统计、上下文进度、缓存拆分、当前提问请求明细和各轮摘要。
- 页面负载升级为 schema v2：只包含数字、时间标签和轮次摘要；当前轮请求明细最多保留最近 100 条，不包含会话路径、线程 ID 或用户消息正文。
- 详情弹层使用按需渲染、数据版本去重和长会话请求上限，避免监控心跳重复重建 DOM。

## v1.2（2026-08-14）

### 自启方案重构
- 开机自启改为「计划任务（登录触发）」唯一方式，移除启动文件夹快捷方式方案（旧版遗留快捷方式会在安装时自动清理）。
- 守护进程新增单实例锁，避免重复拉起。

### 可观测性
- 新增守护进程运行日志 `%LOCALAPPDATA%\ccm-token-spend\guardian.log`：记录检测 Codex、启动 / 停止 / 重启监控、失败原因。
- 守护进程的启动 / 停止错误不再静默忽略，统一写入日志，便于排查面板「等待数据」。

### 文档
- README 更新自启安装说明与排查指引。
- 新增本更新记录文件（后续版本在此增量追加）。

## v1.1（2026-08-06）

- 新增开机自启守护进程（当时使用启动文件夹快捷方式）：Codex 启动自动拉起监控、退出自动停止、监控崩溃自动重启。
- 面板：四角缩放、面板与小按钮位置 / 大小记忆、顶部汇总区与底部「更新于」栏常驻、会话历史标题列随宽度拉伸。
- 空白新对话显示 0，不再显示上一个对话的数据；Codex 启动加载期显示 0，加载完成后自动恢复。
- 输入缓存拆分展示：`输入 X（缓存命中 Y，未命中 Z）`。
- 新增英文 README、agent 版安装指南；新增环境测试报告与 Discussion 模板。
- 修复 `--detail` 崩溃、重复小按钮问题。
- exe 版改为内置 Node 运行时（免单独安装 Node.js）。

## v1.0（2026-08-05）

- 首个版本：Codex / ChatGPT 桌面版右下角 Token 消耗统计面板（本轮 / 会话累计 / 请求次数 / 上下文窗口）。
- 监控进程 `token-stats.mjs` 读取 Codex 本地会话日志，通过 CDP（端口 9229）每秒推送数据到页面。
