{
  "name": "tokens-ui-for-codex",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check": "node --check token-stats.mjs && node --check protocol.mjs && node --check tokens-ui-for-codex-panel.js",
    "test": "node --test --test-force-exit test/token-stats.test.mjs",
    "test:live": "node --test --test-force-exit test/live-ui.playwright.test.mjs",
    "perf": "node scripts/perf-baseline.mjs",
    "verify:live": "node scripts/verify-live.mjs",
    "reload": "node scripts/reload-page.mjs"
  },
  "author": "Littps"
}
